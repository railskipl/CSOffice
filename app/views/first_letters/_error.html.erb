<% if @first_letter.errors.any? %>
<div id="error_explanation">
	<h2>
		<%= pluralize(@first_letter.errors.count,"error") %>
		Prohibited this user from being saved:
	</h2>
	<p>
		There were problems with the following fields:
	</p>
	<ul>
	<% @first_letter.errors.full_messages.each do |message|%>
	 <li><%= message%></li>
	 <% end %>

</div>

<% end %>
